block-list-secret-scan: []

skip-check:
  # CKV_TF_1: Ensure Terraform module sources use a commit hash.
  # KEEP GLOBAL: All modules use local source = "../module" — commit hash check is N/A.
  - CKV_TF_1
